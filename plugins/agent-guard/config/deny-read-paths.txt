# Shell case patterns. Blank lines and lines starting with # are ignored.
.env*
# Alternate dotenv layouts. Keep `env.*` source files readable by enumerating
# only known runtime basenames, while protecting reverse-extension forms such
# as local.env and app.env.local. Explicit template markers are handled by the
# shared candidate_is_allowed policy in bin/agent-guard.
*.env
*.env.*
*.envrc
*.envrc.*
env.local
env.dev
env.development
env.prod
env.production
env.stage
env.staging
env.test
env.testing
env.qa
env.ci
env.*.local
envrc.local
envrc.*.local
.flaskenv*
*.pem
*.key
*.pfx
*.p12
id_rsa
id_dsa
id_ecdsa
id_ed25519
.aws/credentials
.aws/config
.npmrc
.pypirc
.netrc
.docker/config.json
.config/gh/hosts.yml
.git-credentials
.ssh/config
.ssh/id_*
.kube/config
.config/gcloud/application_default_credentials.json
.config/gcloud/credentials.db
credentials.json
secrets.json
secrets.yaml
secrets.yml
service-account*.json
gcp-sa*.json
# Reading the process environment is equivalent to `env`/`printenv`. The Bash
# variant (incl. variable PIDs like /proc/$$/environ) is handled in
# deny-bash-patterns.txt, which the deny-read glob cannot match.
/proc/*/environ
# Additional high-value secret material (keystores, DB/Vault creds, IaC state).
*.p8
*.jks
*.keystore
.pgpass
.my.cnf
*.tfstate
*.tfvars
.vault-token
.dev.vars*
